import { TabSymbol, type TabComponent, type TabProps } from '../types';

/** Declares a named page for the container. The container renders its children. */
const Tab: TabComponent = (() => null) as unknown as TabComponent;
Tab.$$typeofTab = TabSymbol;

export { Tab };
export type { TabProps };
