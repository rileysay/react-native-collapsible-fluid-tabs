// Shared layout constants used across the list wrappers.

/**
 * Extra breathing room added below a tab's content, on top of the tab bar
 * height and the bottom safe-area inset, so the last item never sits flush
 * against the bottom edge. Used by every list wrapper's footer spacer.
 */
export const FOOTER_GAP = 16;
